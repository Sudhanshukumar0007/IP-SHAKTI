--extra-index-url https://download.pytorch.org/whl/cpu

# API framework
fastapi>=0.100.0
uvicorn>=0.23.0

# LangChain ecosystem
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-chroma>=0.1.0
langchain-groq>=0.1.0

# LangGraph — orchestration
# NOTE: requires >=0.2.0 for StateGraph, add_conditional_edges, and END sentinel
langgraph>=0.2.0

# Vector DB
chromadb>=0.4.0

# PDF extraction
pymupdf4llm>=0.0.2

# Embeddings
fastembed>=0.2.7

# Validation
pydantic>=2.0.0

# Utilities
python-dotenv>=1.0.0

# Legacy — kept for backward compatibility if any other code references it
langchain-openrouter>=0.0.1
